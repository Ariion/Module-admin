/* Configuration du module — état de départ.
   En l'état, l'éditeur fonctionne déjà : tout est enregistré dans CE
   navigateur, sans compte à créer. C'est fait pour essayer.
   Pour un vrai site, ouvrez installation.html : la page fabrique la
   configuration Firebase à mettre ici. */
window.ADMIN_CONFIG = {
  siteId: 'mon-site',
  backend: 'demo',
  lang: 'fr',
  debug: true,
  media: { adapter: 'url' },
};
